grumphp:
    tasks:
        phpcs:
            standard: build/phpcs.xml
            whitelist_patterns:
                - /^config\/(.*)/
                - /^routes\/(.*)/
                - /^src\/(.*)/
